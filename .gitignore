# Atomic-write temp file (see saveItems in server.js)
data/*.tmp

# Autogenerated API token (a secret; minted on first boot)
data/token.json

# Node / editor noise (app itself has no deps)
node_modules/
npm-debug.log*
*.log
.DS_Store
.env
